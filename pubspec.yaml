name: book_app
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  google_fonts: ^0.2.0
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your applicatiosn.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  youtube_player_flutter: ^7.0.0+7
  flutter_dialogflow: ^0.1.3
  bubble: ^1.1.9+1
  intl:



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - asset/
 